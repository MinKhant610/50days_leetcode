Question link : https://leetcode.com/problems/plus-one/

Solution :

class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        change_str = str(digits).strip('[]').replace(', ', '')
        operate = str(int(change_str) + 1)
        result = [int(i) for i in operate]
        return result